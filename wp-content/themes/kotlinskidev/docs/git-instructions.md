# Git Workflow

## Branches

- Format: `feature/T{n}-short-description` (e.g. `feature/T15-theme-blocks`)
- Branch off `master`

## Pull Requests

- PRs target `master`
- Title format mirrors the branch: `feat(T{n}): short description`
